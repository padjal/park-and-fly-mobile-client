class Booking{
  final String bookingId;
  final String parkingName;
  final DateTime to;
  final DateTime from;
  final String carNumber;

  Booking(this.bookingId, this.parkingName, this.to, this.from, this.carNumber);
}